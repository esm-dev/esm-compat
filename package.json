{
  "name": "esm-compat",
  "description": "Checks the ES6 compatibility from an `User-Agent` string.",
  "version": "0.2.0",
  "type": "module",
  "module": "dist/compat.js",
  "types": "types/compat.d.ts",
  "scripts": {
    "prepublishOnly": "pnpm test",
    "build": "pnpm esbuild src/compat.ts --outdir=dist --format=esm --platform=browser --target=esnext",
    "test": "pnpm build && node test.mjs"
  },
  "files": [
    "dist/compat.js",
    "types/compat.d.ts"
  ],
  "devDependencies": {
    "@types/ua-parser-js": "0.7.39",
    "esbuild": "0.23.1"
  },
  "dependencies": {
    "ua-parser-js": "^1.0.38"
  },
  "repository": "https://github.com/esm-dev/esm-compat",
  "license": "MIT"
}
